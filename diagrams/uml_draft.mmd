classDiagram
    class Owner {
        +str name
        +List pets
        +add_pet(pet)
        +get_all_tasks()
    }

    class Pet {
        +str name
        +str species
        +int age
        +List tasks
        +add_task(task)
        +get_tasks()
    }

    class Task {
        +str description
        +str time
        +str frequency
        +date due_date
        +bool completed
        +mark_complete()
    }

    class Scheduler {
        +Owner owner
        +sort_by_time()
        +filter_by_pet(pet_name)
        +filter_by_status(completed)
        +detect_conflicts()
    }

    Owner "1" --> "*" Pet : owns
    Pet "1" --> "*" Task : has
    Scheduler --> Owner : reads tasks from